APP_ENV=local

DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=pegaso_web_services_DB
DB_USER=NOMBRE_USUARIO_BD
DB_PASS=TU_PASSWORD_AQUI

DB_CHARSET=utf8mb4

LOG_PATH=logs/error.log

# ── Auth JWT (Mandamiento 14) ─────────────────────────────────
# Generar con: openssl rand -base64 40
JWT_SECRET=CAMBIA_ESTO_POR_UN_SECRETO_ALEATORIO_MINIMO_32_CHARS

# ── PayPal (Fase 1 de pago) ───────────────────────────────────
# Obtener credenciales en developer.paypal.com → My Apps & Credentials
# PAYPAL_MODE: "sandbox" para pruebas | "live" para producción
PAYPAL_MODE=sandbox
PAYPAL_CLIENT_ID=TU_CLIENT_ID_AQUI
PAYPAL_CLIENT_SECRET=TU_CLIENT_SECRET_AQUI

# ── CORS — URL del frontend (whitelist dinámica) ──────────────
# Dev:  dejar vacío (localhost ya está en whitelist de cors.php)
# Prod: FRONTEND_URL=https://pegasoexpediciones.com
FRONTEND_URL=

# ── Frontend Next.js ──────────────────────────────────────────
# Dev:  copiar a booking-engine/.env.local
# Prod: inyectado por GitHub Actions (ver main-deploy.yml)
NEXT_PUBLIC_API_URL=http://localhost/PegasoExpedicionesDev/api
NEXT_PUBLIC_CONTACT_PHONE=521XXXXXXXXXX
